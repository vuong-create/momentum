type PlaceholderPageProps = { title: string; description?: string; eyebrow?: string; }; export default function PlaceholderPage({ title, description, eyebrow, }: PlaceholderPageProps) { return ( <section className="page placeholder-page"> <div className="page-heading"> {eyebrow && <p className="page-eyebrow">{eyebrow}</p>} <h1>{title}</h1> {description && <p>{description}</p>} </div> <div className="placeholder-card"> <div className="placeholder-glow" /> <div className="placeholder-card-content"> <span className="placeholder-label">ROOM UNDER CONSTRUCTION</span> <h2>{title} is coming next.</h2> <p> This space already belongs inside Momentum. We’ll build its full experience here without disturbing the rest of the application. </p> </div> </div> </section> ); }